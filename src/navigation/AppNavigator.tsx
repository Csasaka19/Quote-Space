import React from 'react';
import HomeScreen from '../screens/HomeScreen';

/**
 * Navigation placeholder — will be replaced with React Navigation
 * stack/tab navigator once we install the navigation packages.
 *
 * Flutter parallel: This is like your MaterialApp's `routes:` map
 * or GoRouter configuration. React Navigation uses a similar
 * declarative approach with <Stack.Navigator> and <Tab.Navigator>.
 */
const AppNavigator = () => {
  return <HomeScreen />;
};

export default AppNavigator;
